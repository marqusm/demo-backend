version: '3'
services:

  postgres:
    image: postgres:12.3
    restart: always
    stop_signal: SIGINT
    environment:
      - POSTGRES_DB=demo_backend
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
